<h1>Stacks</h1>

<div class="stacks-list">
  <% @stacks.each do |stack| %>
    <div class="stack-card">
      <h2><%= link_to stack.name, stack_path(stack) %></h2>
      <p class="stack-category"><%= stack.category %></p>
      <p><%= stack.description %></p>
    </div>
  <% end %>
</div>
